<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.simcoder.tinder">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="henho.ketban.hete.Login.LauncherActivity"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="henho.ketban.hete.Login.RegisterActivity" />
        <activity android:name="henho.ketban.hete.Login.LoginActivity" />
        <activity android:name="henho.ketban.hete.Login.ChooseLoginRegistrationActivity" />
        <activity android:name="henho.ketban.hete.EditProfileActivity" />
        <activity android:name="henho.ketban.hete.Chat.ChatActivity" />
        <activity
            android:name="henho.ketban.hete.MainActivity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme" />
        <activity android:name="henho.ketban.hete.ZoomCardActivity" />
        <activity android:name="henho.ketban.hete.SettingsActivity"></activity>
    </application>

</manifest>